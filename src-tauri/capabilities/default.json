{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main application window permissions. Remote device pages are deliberately excluded: guest pages never receive IPC access.",
  "windows": ["main"],
  "local": true,
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default"
  ]
}
